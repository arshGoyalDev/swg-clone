import { initiativesData, blogsData, teamData, resourcesData, eventsData } from "./data";
import { cvData } from "./cvData";

export { initiativesData, blogsData, teamData, resourcesData, cvData, eventsData };
